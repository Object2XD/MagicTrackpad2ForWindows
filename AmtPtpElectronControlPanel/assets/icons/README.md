# Icons

Place production installer icons here before packaging:

- `app.ico` for the Windows executable and NSIS installer.
- Optional `tray.ico` if you want to replace the generated tray glyph in `src/main.js`.

The current shell intentionally uses a generated tray image so the app can run before artwork is finalized.
